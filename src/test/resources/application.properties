# Tests get their own database in memory, so running them never touches - or
# wipes - the characters in ./data.
spring.datasource.url=jdbc:h2:mem:web-mmo-test;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
